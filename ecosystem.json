{
  "apps": [
    {
      "name": "goflashdeal-frontend",
      "script": "yarn start --port=8888",
      "env": { "COMMON_VARIABLE": "true" },
      "env_production": { "NODE_ENV": "production" }
    }
  ],
  "deploy": {
    "production": {
      "user": "root",
      "host": ["82.157.68.83"],
      "ref": "origin/dev-opacity",
      "repo": "git@git.pincman.com:taoke/web.git",
      "path": "/home/pincman/goflashdeal-frontend",
      "post-deploy": "yarn install && yarn build && pm2 startOrReload ecosystem.json --env production",
      "ssh_options": "StrictHostKeyChecking=no",
      "env": { "NODE_ENV": "production" }
    }
  }
}
